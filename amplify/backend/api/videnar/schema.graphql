type Question @model {
  id: ID!
  title: String!
  answers: [Answer] @connection(keyName: "byQuestion", fields: ["id"])
}

type Answer @model @key(name: "byQuestion", fields: ["questionID"]) {
  id: ID!
  title: String!
  questionID: ID!
  question: Question @connection(fields: ["questionID"])
}
